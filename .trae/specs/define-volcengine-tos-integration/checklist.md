- [x] 前端全局配置中心已定义火山引擎对象存储配置项，包含 `region`、`bucketName`、`host`、`accessKeyId`、`secretAccessKey`
- [x] 规格明确对象存储配置为独立新配置项（与素材库配置并列），浏览器可直接持有长期 `AK/SK`，并在上传时传给后端
- [x] 规格明确对象存储配置从浏览器本地读取，缺失时回退原有逻辑并继续使用生成资源的原始链接
- [x] 规格明确首期只覆盖资源库上传、重新生图和删除资源，不包含历史数据迁移
- [x] 规格明确资源库上传文件和重新生图结果统一写入对象存储
- [x] 规格明确新增主链路禁止继续依赖大体积 `base64` 传输，并给出兼容过渡要求
- [x] 规格明确采用公网读与固定 `host` 访问资源，并支持基于元数据重新生成地址
- [x] 规格明确对象路径命名规则为 `manju/漫剧项目ID/季ID/类型英文/资源`，类型包含 `role`、`scene`、`prop`、`audio`、`video`、`shot`
- [x] 规格明确对象文件名采用 `资源ID + 时间戳 + 扩展名`
- [x] 规格明确删除资源和重新生图时需要同步删除桶内旧对象，并定义失败可追溯与补偿要求
- [x] 规格明确删除策略为先删业务引用，再异步调用删除接口清理桶中旧对象，且不要求等待删除确认；删除失败仅记日志，不进入重试队列
- [x] 规格明确删除策略为先删业务引用，再异步调用删除接口清理桶中旧对象，且不要求等待删除确认
- [x] 规格明确上线前联调检查项，覆盖配置读取与回退、上传入桶、固定 `host` 访问、删除异步清理和失败日志追溯
- [x] 规格明确上线前回归检查项，覆盖首期范围边界、历史数据不迁移、旧逻辑回退与 `base64` 主链路治理
- [x] 规格明确上线前发布检查项，覆盖灰度策略、监控告警、回滚条件与发布后验证步骤
